#include "push_swap.h";

void ft_sort_min(t_stack  *a , t_stack *b , t_node **array , int count)
{
    if(count == 2)
        sa(a);
    else if(count == 3)
        sort_size_3(a);
    else if(count == )
    else if(count == 6)
        sort_size_4(a , b, count);
}